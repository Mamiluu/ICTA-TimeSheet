# Copy to .env and fill in real values. Never commit .env.
DATABASE_URL="postgresql://user:password@host:5432/icta_attendance?sslmode=require"

# The public URL this API is served from (used in CORS and in activation/reset email links).
PUBLIC_APP_URL="http://localhost:3000"

# Gmail address + App Password (not your normal password -- generate one at
# https://myaccount.google.com/apppasswords) used to send activation/reset emails.
GMAIL_SENDER_ADDRESS="msanifuasiya@gmail.com"
GMAIL_APP_PASSWORD="xxxx xxxx xxxx xxxx"

PORT=3000
NODE_ENV=development
