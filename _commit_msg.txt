Fix dead privacy badge and add a real, substantive privacy policy page

- index.html referenced document.getElementById('privacyBadge') and had
  a fully-styled .privacy-badge CSS class, but no such element actually
  existed in the markup -- the "your entry is sealed" trust message
  (guarded by an if(privacyBadge) check, so it silently no-op'd rather
  than erroring) has never rendered for any attendee. Added the missing
  element with real copy, plus a link to the new policy page.
- privacy.html: an actual, specific privacy policy -- what's collected
  (name/org/email/phone/signature/consent), the sealed-roster access
  model, why entries aren't hard-deleted (audit-trail integrity, same as
  a paper register), where data is stored, and a plain-language nod to
  Kenya's Data Protection Act 2019's principles (data minimization,
  purpose limitation) without claiming a formal compliance
  certification. Branding-aware via the same GET /api/branding used
  elsewhere.
- Linked from index.html's footer and login.html's footer-note.
